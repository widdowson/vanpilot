# Bazel output directories (symlinks)
bazel-*

# Git worktrees (see PARALLEL_STRATEGY.md)
/worktrees/

# Python bytecode
__pycache__/

# Sandbox proxy CA cert (build-time only, not committed)
supervisor/proxy-ca.crt

# Release keystore (never committed — use env vars for signing)
android/keystores/release.keystore

# Video captures from golden tests (AC-4.3: never committed)
*.mp4

# Docker sandbox runtime data
sandbox/sandbox.env
.sandbox/
